// Static-site generation for Cloudflare Pages: prerender every route at build
// time. No server runtime / wrangler needed — the output is plain static assets.
export const prerender = true;
export const trailingSlash = 'ignore';
